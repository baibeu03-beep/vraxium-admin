import ResetPasswordForm from "@/components/admin/ResetPasswordForm";

// 비밀번호 재설정 완료(새 비밀번호 입력) 화면 — /auth/recovery 경유로 진입.
export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
