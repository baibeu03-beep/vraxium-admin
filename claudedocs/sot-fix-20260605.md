# SoT 정리 수정 보고서 — 2026-06-05

SoT 감사(`sot-audit-20260605.md`) 후속 — PMS 실제 이관 Apply 전, snapshot-only 조회 구조를
깨는 잔여 자체 계산/독립 구현 정리. **수정 대상은 전부 front repo(../vraxium, branch Tuna),
admin repo 코드 변경 없음.**

## 변경 파일 (front repo)

### 1. `app/(host)/api/cluster4/weekly-growth/route.ts` (−180/+33줄)
- **사용처 전수 조사 결과**: 라이브 소비처는 `components/cluster-4/Cluster4Content.tsx`(=/cluster-4-1 진입 화면) 단 1곳,
  사용 필드는 `data.seasonSummary` / `data.seasonPointSummary` / `data.seasonSummaries` 3개뿐.
  Career-Resume 폴더는 루트 `app/` 밖 sibling이라 **라우팅되지 않는 dead code**(빌드 미포함).
- **폐기(사용처 0 + 정본 불일치 실증)**: `weeklyCards`(buildWeeklyCards 독립 구현),
  `growthStats`(raw uws 카운트 — 미공표·전환 포함), `currentWeekInfo`(weeks.is_official_rest 직독),
  `userGrowthStatus`/`userStatus`. 응답 = `{ data: { seasonSummary, seasonPointSummary, seasonSummaries } }`.
- **admin 정본 프록시 미채택 사유**: admin getWeeklyGrowth 는 LIVE 10s+ 계산(감사 실측 9.6~10.1s) —
  진입 경로에 얹으면 front 이력서 graft 8s 타임아웃 사고와 동일 클래스. 유지된 시즌 3필드는
  이미 admin SoT 정렬(판정=admin /api/cluster1/resume seasonRecords graft, 포인트=user_weekly_points
  직접 합산 — 이력서와 동일 원장)이고 감사에서 admin 정본과 값 일치 실증됨.
- 부수 효과: 진입 화면 API가 무거운 buildWeeklyCards/uws 전수 조회를 더는 하지 않음(지연 감소).

### 2. `components/cluster-4/Cluster4Content.tsx` (자체 계산 제거)
- 누적 인정 주차 클라이언트 자체 계산(raw uws success + 온보딩 +1, 미공표·전환 포함 — 정본 27 vs 26 실증) 제거.
- **정본 직독**: `/api/cluster4/weekly-cards`(snapshot proxy)의 현재 주차 카드
  `accumulatedApprovedWeeks` 사용. area-6-circles 로드와 동일 URL → `dedupedJson` 캐시 공유(추가 네트워크 0).
- 폴백: 현재 주차 카드 미존재 시 전체 카드 max(acc 단조증가라 동치), 조회 실패 시 0(fail-closed, 종전 동일).
- 온보딩 주차는 `Math.max(acc, 1)` — eligible_min=1 주차범위 게이트가 첫 주에 닫히지 않게 기존 표시 규칙 유지.
- 이 값은 라인 주차범위 게이팅(`eligible_min/max_approved_weeks` inRange) 입력.

### 3. `app/(host)/api/profile/summary/route.ts` (게이트 적용)
- weeks 조회에 `result_published_at` + `season_definitions(season_type)` 추가.
- **시즌별 성공 주차(seasonSuccessWeeksMap)**: success ∧ 공표 완료 ∧ 비-break ∧ 비-전환 —
  `confirmed-success-weeks`(/api/crews)·admin stats-cards 와 동일 규칙.
- **reliability 분류 루프**: 전환 주차 = 분자·분모 제외, 미공표 주차 = 성공/미이행 어느 쪽으로도 확정 안 함(분모 제외).
- 시즌 `total_weeks` 전환 주차 제외(16/8 정책, 이력서와 동일).
- 온보딩 dedup 결함 수정: 존재하지 않는 `wg.week_id`/`wg.is_success` 참조(항상 +1) →
  `status==='success' && week_start_date===온보딩 주차 시작일` 로 교정.
- 응답 계약(top-level 키) 불변.

## 제약 준수
- snapshot-only 유지: snapshot 생성/조회/DTO version(18) 일절 미변경. 모든 수정은 read 경로.
- demoUserId/일반 동일 DTO: weekly-cards 경로 미변경, Cluster4Content 신규 fetch 는 area-6 와 동일 URL 규칙(demoQS 포함).
- DTO/API 계약: 소비처가 쓰는 필드 형태 불변(weekly-growth data.*, summary top-level 키 동일 — 검증 통과).
  weekly-growth 의 폐기 키는 라이브 소비처 0 확인 후 제거(사용자 지시 "사용처 없으면 폐기").
- checks_migrated/check 값: 일절 미접촉.

## 검증 (scripts/verify-sot-fix-20260605.mjs · verify-sot-fix-browser.mjs)
환경: 로컬 admin(:3000)+front(:3001) dev — 둘 다 운영 Supabase. 유저: tester e4dcb97e(42주)·real 247021bc.

| 항목 | 결과 |
|---|---|
| weekly-growth 응답 키 = data 만 (폐기 확인) | ✅ 2유저 |
| data.seasonSummary/seasonPointSummary/seasonSummaries 로컬==운영 | ✅ 전부 일치 (무회귀) |
| summary 응답 계약(top-level 키) 로컬==운영 | ✅ |
| summary approved_weeks == direct 정본(confirmed 규칙 재계산) | ✅ (단, 아래 발견 참조) |
| 게이팅 입력 acc — local front weekly-cards 현재주(봄 W14) | ✅ **26** (= admin 정본, 구 자체계산 27) |
| 브라우저 /cluster-4-1 진입: 시즌 타이틀·상태 배지 렌더, weekly-growth/cards 200, 관련 콘솔 에러 0 | ✅ (포인트 469/45/-14 = admin 정본 일치) |
| 브라우저 /cluster-4 카드목록: 렌더·weekly-cards 200 (영향 없음 확인) | ✅ |
| snapshot 영향 | 없음 (read 경로만, 재계산 불필요) |

### 검증 중 발견 (별도 조치 대상, 이번 범위 밖)
- **/api/profile/summary 의 시즌 이력·신뢰도 블록은 운영에서 이미 전 유저 dead**:
  라우트의 레거시 select 가 운영 스키마와 불일치 — `user_season_histories.progress_status`,
  `seasons.year` 컬럼 부재로 42703 에러 → `seasonHistories=[]`, `reliabilityRate=0` (운영도 동일).
  메인 사이드바(home-career)는 `/api/profile`(confirmed-success-weeks 적용 완료) 사용이라 실사용 영향 없음.
  summary 의 유일 라이브 소비처 = index-two 데모 페이지(home-two Sidebar). → 이번 게이트 수정은
  계약 보존·무회귀이며, 라우트 자체의 레거시 스키마 정리(또는 폐기)는 후속 과제.
- **라우트-컴포넌트 교차 매핑**: `/cluster-4` → `components/cluster-4-1/Cluster41Content`(카드목록),
  `/cluster-4-1` → `components/cluster-4/Cluster4Content`(진입). 폴더명과 라우트가 반대.
- `lib/cluster4-weekly-cards.ts`(buildWeeklyCards)는 front repo 의
  `app/(host)/api/admin/crews/[legacy_user_id]/cluster4/weekly-growth`가 여전히 import —
  admin repo 에 동일 정본 엔드포인트가 있으므로 front 사본의 폐기 검토는 후속 과제.

## 타화면 영향
- /cluster-4(카드목록)·이력서 sidebar(/api/profile)·/api/crews·admin 화면: 데이터 소스 미변경 — 영향 없음.
- /cluster-4-1 진입: 시즌 요약 3필드 값 불변(무회귀 검증) + 활동 보드 주차범위 게이팅만 정본화(27→26).
- index-two 데모 페이지: summary 소비 — 종전부터 빈 값/0, 시각적 변화 없음.
