# §12 Dry Run 보고서 — pms 1092 장승완 이관 파이프라인 검증

> **작성일**: 2026-06-06 · **모드**: DRY-RUN — **실제 INSERT/UPDATE/DELETE 0건** (pms SELECT + Supabase select + HTTP GET만)
> **스크립트**: `scripts/dryrun-pms-1092.ts` → `claudedocs/dryrun-pms-1092-20260606.json` (주차별 전체 명세 weekRows 포함)
> **원칙 준수**: snapshot-only 불변 · checks_migrated 행 단위 플래그만 판정 SoT(분포/크기 추론 없음) · DTO/API/snapshot 로직 무변경 · demoUserId=동일 로더

## ⭐ 핵심 재현율 검증 — 통과

**계획 uws success 14주 = pms usersinfo.Week 14 — 누적 인정 주차 완전 재현** (A3-⑧ Week=COUNT 계약 실증. 1092는 Week 불일치 15명에 비포함 확인).

## ① 동일인 매칭

| 항목 | 결과 |
|---|---|
| pms | 1092 장승완 / 980416 / 010-6258-4153 / 1443beacon@gmail.com / 아주대 영문학과 / F&B·일반 / State=일반 / StartDate 2025-03-24 |
| Vraxium 동명 후보 | **0명** (pms 내 동명도 1명 — 모호성 없음) |
| legacy_user_id 브리지 | 기존 0건 |
| **확정 계획** | **신규 채번** — users(uuid+legacy_user_id=1092) + user_profiles 신규. ⚠️ 신규 유저 organization_slug 결정 규칙(F&B→?) 미정의 — 5명 확대 전 매핑 사전 필요 |

## ② 프로필 매핑 — 신규 생성 (충돌 diff 없음, 기존 값 보존 원칙은 무대상)

pms users/usersinfo → user_profiles(display_name·birth_date·contact·school) + user_memberships(F&B/일반/일반) + user_educations(아주대/영문학과). 전부 신규 행 — 운영 사용자 충돌 없음.

## ③ pointlogs → legacy_point_ledger

| 항목 | 값 |
|---|---|
| 적재 행 | **510** (alive 495 + **voided 15** — A3-⑤ 쌍 보존, alive 선별 금지) |
| code '0000' | 1행 포함 (제외 금지 A3-⑦) · date_substituted 0 |
| 멱등 키 | (source_table='pointlogs', source_pk=LogNum) UNIQUE |
| ⚠️ 선행 조건 | **legacy_point_ledger DDL 미적용** — SQL Editor 수동 적용 필수 (§10 미완 항목, apply 차단 게이트) |

## ④⑤ uwp / uws 생성 계획 (§5-1 확정식)

| 테이블 | 계획 |
|---|---|
| user_weekly_points | **46행 전부 insert** (기존 행 0 — 신규 유저라 덮어쓰기/충돌 없음). 14일 보호(~2025-04-07) 0건 발동 · 미귀속 pointlogs **47행**(시즌 사이 갭) → 보류 큐·adjustment 흡수 |
| user_week_statuses | **27행 전부 insert** (success 14 / fail 13) · 상태 충돌 0 · 활동 귀속 실패 2행(보류 큐) |
| 시즌 분포 | 2025-spring uwp9/uws7 · **2025-summer uwp4/uws3** · 2025-autumn uwp13/uws10 · 2026-winter uwp9/uws7 · 2026-spring uwp11/**uws0**(활동 중단, 포인트 로그만 존재) |
| seasons/weeks 참조 | **신규 생성 0** — B7 백필 153주가 전부 커버 (2025-spring~2026-spring 5개 시즌 46주 참조) |

## ⑥ checks_migrated + 판정 diff (PMS 인정 vs v18)

기본 계획: uwp 46행 전부 `checks_migrated=true`. 판정 비교(활동 27주):

| 구분 | 수 | 상세 |
|---|---|---|
| 일치 | **24** | |
| **FLIP** (pms 인정 → v18 fail) | **2** | `2025-autumn W13`: rating 7, check 22 < thr 30(비감사 주차—confirmStar 부재) · `2026-winter W5`: rating 10, check 24 < 37 — 양쪽 다 pms 수동 인정 흔적 (B8 Part B 클래스) |
| 역방향 (pms fail → v18 pass) | 1 | 2025-summer W4 (thr 0) — 게이트는 승격 없음, uws=fail 유지 → **표시 영향 없음** |

**정책 옵션**: PMS 인정 우선(권장) = FLIP 2행만 `checks_migrated=false` → 화면 뒤집힘 0 / fail 수용 = 표시 누적 14→12.

## ⑦ [통합] 실무 경험 계획 (v17, ≤ 2026 봄 W16)

| 항목 | 값 |
|---|---|
| 라인 신규 생성 | **0** (기존 active 통합 마스터/라인 재사용 — v17 계약) |
| targets / submissions / evaluations | **27 / 27 / 27** (전 주차 subtitle=Activity 원문 보유·rating=Star 보유, 결측 0) |
| 복수 활동 주차 | rating=max(Star)·subtitle=최장 원문 병합 (전 행 ledger 보존) |

## ⑧ adjustment (§5-2) — 오늘 잔액 기준 (apply 시 컷오버 직전 재계산 필수)

| 항목 | PMS 원본 | 주차 집계 합 | **delta (sentinel 1900-W0)** |
|---|---|---|---|
| Star | **925** | 885 | **+40** (points) |
| Shield | **−54** | adv 합−pen 합 = −79 | **D=+25** → advantages 25 / penalty 0 |

- 적용 후 항등식 자동 성립: Σpoints=925 · Σ(adv−pen)=−54. sentinel 은 checks_migrated=false + weeks 부재 → 주차 판정·카드 비오염(§5-2 소비처 검증 계약).
- A2 분석(06-05) 기대 +8 과의 차이: pms 운영 계속(잔액 변동) + **미귀속 47행 흡수분** 포함 — adjustment 가 잔차를 닫는 구조라 항등식은 항상 성립.

## ⑨ snapshot 영향

- 현재 snapshot **없음**(신규 유저) → 이관 시 신규 생성 1개 필요 (재계산 = 파이프라인 7단계 명시 수행, lazy 아님).
- 카드 수 예상: **27장** (uws 행 주차만 — uwp-only 주차 19개는 카드 미생성).
- 기존 122 snapshot 무영향 (타 사용자 무접촉).

## ⑩ rollback 계획

evaluations/submissions/targets(멱등키) 삭제 → uws 27 삭제 → uwp 46+sentinel 삭제 → ledger 510 삭제 → users.legacy_user_id 제거 → (신규 유저라 profile 행 삭제) → recalc+snapshot 삭제 → 이관 전 상태 diff=0. apply 구현 시 B7 패턴(run log·prior 기록·현재값 가드) 재사용.

## 검증 필수 6항

| # | 항목 | 결과 |
|---|---|---|
| 1 | direct | 대상 uuid 미존재(신규 채번 경로) — **부재 baseline**: direct 로더와 동일 응답 형태 확인. apply 후 실측 항목으로 이월 |
| 2 | HTTP | ✅ 운영 admin(internal)·front proxy 모두 200 — 부재 uuid 에 `data:[]` 동일 DTO 골격 |
| 3 | direct == HTTP | ✅ 부재 동작 양 경로 일치 (admin internal == front proxy, status·body 구조 동일). **데이터 보유 사용자의 양 경로 deep equal 은 금일 B7 검증에서 기실증** — 이관 후 1092 uuid 로 동일 하네스 재실행 계약 |
| 4 | snapshot 영향 | 현재 0 (snapshot 부재·타 사용자 무접촉) |
| 5 | snapshot 재계산 필요 | 이관 apply 시 **신규 생성 필수** (명시 재계산 — 파이프라인 강제 단계) |
| 6 | 브라우저 예상 | front `/cluster-4?userId=<신규uuid>`: 카드 27장(봄7·여름3·가을10·겨울7) · 누적 인정 14주(PMS인정우선 채택 시 — fail수용 시 12) · 방패=Σadv−pen+sentinel=−54+α 표기 정책 확인 필요 · 단감=주차별 points · summer 3장은 공표 상태라 확정 표시 |

## ⚠️ 발견 사항 (5명 확대 전 처리)

1. **legacy_point_ledger / legacy_event_logs DDL 미적용** — apply 차단 게이트 (SQL Editor 수동).
2. **신규 채번 유저 organization_slug 매핑 규칙 부재** (pms Team "F&B" → Vraxium org?) — 매핑 사전 필요.
3. **2025-summer 합성 주차 교차**: 1092 카드가 summer 3장 생성 — 테스터 충족용 주차(threshold 0·publish 자동)에 실사용자 데이터가 실림. pms 는 summer **미공표**(IsPublic=0)인데 live 는 공표 → 확정 표시됨. threshold 0 이라 게이트 무력(검증상 무해)이나 **summer 8주 속성 재정렬 결정(이월 항목)과 충돌 지점 — 5명 확대 전 확정 권장**.
4. FLIP 정책 서면 확정 필요 (PMS 인정 우선 권장 — 1092 는 2행).
5. 미귀속 보류 큐: pointlogs 47행(시즌 갭 — adjustment 흡수로 잔액 무손실)·활동 2행 — 건수 보고 계약 충족.

## 다음 단계 — 5명 확대 가능 여부

**Dry-run 확대: 즉시 가능** — 본 스크립트의 PMS_USER_ID 일반화(인자화)만으로 대조군 포함 5명(§12-3: 1299 지서연 부분보정 · 완전일치 1명 · 졸업/정지/휴식/펜딩 보유 각 1명) 실행 가능.

**Apply 진입: 조건부** — 선행 4건: ① ledger DDL 수동 적용 ② FLIP 정책 확정(PMS 인정 우선 권장) ③ org 매핑 사전 ④ summer 8주 속성 결정. 1명 apply → 검증(direct/HTTP/demoUserId/브라우저 실측) → 롤백 리허설 1회 → 5명.
